# Final Verification Report

Generated: 2026-09-17

## Commands executed

| Command | Result |
|---------|--------|
| `pip install -e ".[dev]"` | Pass |
| `ruff format app tests` | Pass |
| `ruff check app tests` | Pass (B008 ignored for FastAPI Depends) |
| `pytest -q` | **13 passed** |
| `npm test -- --run` | **3 passed** |
| `npm run build` | Pass |
| `uvicorn` + `GET /api/health` | `status=ok` |
| `GET /api/providers/health` | `status=ok` (mock + local stores) |
| `python -m app.cli extract-all ...` | 5 JSON outputs written |
| `python -m app.cli validate-output ...` | 5/5 valid |
| `evals/run_evals.py` | schema_valid=5, hallucination_count=0 |

## Sample outputs

- `outputs/sample/1.Policy_Copy.json`
- `outputs/sample/GHI_Policy.json`
- `outputs/sample/Net_Catalyst_-_GPA_-_Policy_Copy_-_2022-23.json`
- `outputs/sample/Policy_liberty_2022-2023.json` (duplicate of Net Catalyst)
- `outputs/sample/olj4KTUo9B1546-1692687606_925469_-_00_GMC_Renewal_Policy_00.json`

All generated by the pipeline with `LLM_PROVIDER=mock`. Non-null fields (when present) include page evidence; unsupported fields remain `null` / `unknown`.

## Configuration used for verification

```
LLM_PROVIDER=mock
VECTOR_STORE=local
GRAPH_STORE=local
EMBEDDING_PROVIDER=hash
OCR_ENABLED=false
```

Pinecone / Neo4j / OpenAI were not configured for this run; adapters are implemented and fail clearly when selected without credentials.

## Evidence / accuracy claims

- No hardcoded sample answers found in extraction outputs
- Hallucination count (non-null without evidence): **0**
- Accuracy percentage: **not claimed** (mock mode does not perform full semantic extraction)

## Secret scan

No committed API keys in application source or `.env.example`. Runtime `.env` is gitignored.
